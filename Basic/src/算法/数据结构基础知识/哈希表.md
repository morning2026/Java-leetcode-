# 哈希表 Hash Table
根据关键码的值而直接进行访问的数据结构，用来快速判断一个元素是否出现在集合里，牺牲空间换取时间
## 哈希函数
把数据映射为哈希表上的索引
HashFuction = HashCode(data)%tableSize
## 哈希碰撞
映射到了同一个索引上
### 链表法
在该索引处建立一个链表存储所有发生哈希碰撞的数据
需要选择适当的哈希表大小，既不会因为控制太多浪费内存，又不会因为链表太长查询耗时
### 线性探测法
寻找空位存放哈希碰撞的数据
tableSize>DataSize
## 常见的三种哈希结构
数组、集合（Set）、映射（map）

## 例题
### 两个数组的交集
//方法1：将结果集合转为数组
`return res.stream().mapToInt(Integer::intValue).toArray();`
将 Set<Integer> 转换为 int[] 数组：
1. stream() : Collection 接口的方法，将集合转换为 Stream<Integer>
2. mapToInt(Integer::intValue) :
    - 中间操作，将 Stream<Integer> 转换为 IntStream
    - 使用方法引用 Integer::intValue，将 Integer 对象拆箱为 int 基本类型
3. toArray() : 终端操作，将 IntStream 转换为 int[] 数组。
### 快乐数
Sum会重复出现，一旦Set中有就失败
### 四数相加二
相当于合成为两个数组相加，用HashMap存储，键是两个数组的和，值是和出现的次数（注意这里不需要给索引）
遍历num3和num4，求和，如果有数组1的相反数，满足，答案++
### 赎金信383
如果a.length>b.length，直接返回false
用数组计算b中每个字母出现的次数，然后遍历a,出现负数时说明false
没出现就是true
